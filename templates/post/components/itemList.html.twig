<section class="post-item-list">
    <div class="row">
        {% for post in posts %}
            <div class="col-12  mb-3 {{ size == "small" ? "col-md-6 col-lg-3" : "col-md-6" }}">
                <article>
                    <a href="/blog/{{post.id}}" class="text-dark text-decoration-none">
                        <img class="w-100 {{size}}" src="{{post.thumbnail}}" alt="{{post.title}}" loading="lazy">
                        <h2 class="my-2">
                            {{post.title}}
                        </h2>
                        <p>
                            {{post.excerpt}}
                        </p>
                    </a>
                </article>
            </div>
        {% endfor %}
    </div>
</section>